(ns codeRedux.views.welcome
  (:require [codeRedux.views.common :as common]
            [codeRedux.views.articles :as articles]
            [codeRedux.views.generalInfo :as generalInfo]
            [codeRedux.views.cssdemos :as cssdemos]
            [codeRedux.views.pokerBots :as bots])
  (:use [noir.core]
        [hiccup.core]))

